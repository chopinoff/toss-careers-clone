'use client';

import { JobsItem } from '@/app/_types/jobs.types';
import { styled } from 'styled-components';

export default function JobsItem({ title, tag, badge }: JobsItem) {
  return <Wrapper></Wrapper>;
}

const Wrapper = styled.div``;
